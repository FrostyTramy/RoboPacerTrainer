# Training frames (large image dataset — keep driving_log.json, exclude images)
frames/

# Compiled model binaries
models/*.hef
models/*.har
models/*.onnx
models/*.pth
models/*.npy

# Calibration data
calib_data*.npy

# Hailo SDK / Docker installer packages (large binaries)
engine/resources/*.whl
engine/resources/*.zip

# Python cache
__pycache__/
*.pyc
*.pyo

# Logs
*.log
